function findOptimalBlock(blocks, amenities) {
  // minWindowSize
  // winningBlock
  // left pointer
  // right pointer
  // amenities = map(amenities)
  // while sum of amenities values < amenities.length
  // for each amenity, if blocks[r] has it, increment count in map
  // if each value in amenities is > 0, update winning block
  // move right pointer
  // while each value in amenities is > 0
  // if window size is smaller than minWindowSize
  // update minWindowSize
  // increment left
  // return winningBlock
}
