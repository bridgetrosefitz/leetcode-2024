Function findDuplicateSubtrees(root) {
	Const ans = []
	Const subtrees = new Set([4], [4,2], [4,2,1])	
	Const currentTree = [4, 2, 1]

	dfs(node) {
		
		// when node is null
			// add null to currentTree
			// return
		// visit left node
		// add node value to currentTree
		// check if tree is already in Set
			// add node to ans
		// add currentTree to Set
		// visit right
}
}

