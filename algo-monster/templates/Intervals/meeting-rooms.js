function canAttendMeetings(intervals) {
  // if 1 or 0 intervals, return true
  // sort ascending by start time
  // set up first interval for comparison
  // starting at second, iterate over every interval
  // >> do the 'interesting part', e.g. checking for overlap (return false if overlap)
  // >> pull out current start and end values
  // return true
}
